SELECT blocked.pid AS blocked_pid, blocked.usename AS blocked_user,blocked.query AS blocked_query,blocked.state AS blocked_state,blocking.pid AS blocking_pid,blocking.usename AS blocking_user,blocking.query AS blocking_query,blocking.state AS blocking_state,age(now(), blocked.query_start) AS blocked_duration FROM pg_catalog.pg_locks blocked_locks JOIN pg_catalog.pg_stat_activity blocked ON blocked.pid = blocked_locks.pid JOIN pg_catalog.pg_locks blocking_locks ON blocking_locks.locktype = blocked_locks.locktype AND blocking_locks.DATABASE IS NOT DISTINCT FROM blocked_locks.DATABASE
 AND blocking_locks.relation IS NOT DISTINCT FROM blocked_locks.relation
 AND blocking_locks.page IS NOT DISTINCT FROM blocked_locks.page
 AND blocking_locks.tuple IS NOT DISTINCT FROM blocked_locks.tuple
 AND blocking_locks.virtualxid IS NOT DISTINCT FROM blocked_locks.virtualxid
 AND blocking_locks.transactionid IS NOT DISTINCT FROM blocked_locks.transactionid
 AND blocking_locks.classid IS NOT DISTINCT FROM blocked_locks.classid
 AND blocking_locks.objid IS NOT DISTINCT FROM blocked_locks.objid
 AND blocking_locks.objsubid IS NOT DISTINCT FROM blocked_locks.objsubid
 AND blocking_locks.pid != blocked_locks.pid
JOIN pg_catalog.pg_stat_activity blocking 
 ON blocking.pid = blocking_locks.pid
WHERE NOT blocked_locks.granted
ORDER BY blocked_duration DESC;


✅ Đã hiểu!
Bạn muốn query Blocking Locks (query bạn vừa paste) chạy tự động mỗi 1 phút 1 lần và hiển thị kết quả thời gian thực (live) trong Grafana.
Dưới đây là hướng dẫn chi tiết, chính xác từng bước (áp dụng ngay cho query bạn đưa):
1. Tạo / Chỉnh sửa Panel (nếu chưa có)
Mở Dashboard của bạn.
Nhấn Add panel (hoặc Edit panel Blocking Locks nếu đã có).
Chọn Data source là PostgreSQL (Postgres Test).
Paste nguyên query bạn đưa vào ô Query (hoặc dùng query đã format đẹp hơn ở dưới để dễ đọc).
Query đã format sạch + thêm 2 cột hữu ích (khuyến nghị dùng luôn):
SELECT 
    blocked.pid                  AS blocked_pid,
    blocked.usename              AS blocked_user,
    blocked.query                AS blocked_query,
    blocked.state                AS blocked_state,
    blocking.pid                 AS blocking_pid,
    blocking.usename             AS blocking_user,
    blocking.query               AS blocking_query,
    blocking.state               AS blocking_state,
    age(now(), blocked.query_start) AS blocked_duration,
    blocked.wait_event_type,
    blocked.wait_event
FROM pg_catalog.pg_locks blocked_locks
JOIN pg_catalog.pg_stat_activity blocked ON blocked.pid = blocked_locks.pid
JOIN pg_catalog.pg_locks blocking_locks 
    ON blocking_locks.locktype = blocked_locks.locktype
   AND blocking_locks.database IS NOT DISTINCT FROM blocked_locks.database
   AND blocking_locks.relation IS NOT DISTINCT FROM blocked_locks.relation
   AND blocking_locks.page IS NOT DISTINCT FROM blocked_locks.page
   AND blocking_locks.tuple IS NOT DISTINCT FROM blocked_locks.tuple
   AND blocking_locks.virtualxid IS NOT DISTINCT FROM blocked_locks.virtualxid
   AND blocking_locks.transactionid IS NOT DISTINCT FROM blocked_locks.transactionid
   AND blocking_locks.classid IS NOT DISTINCT FROM blocked_locks.classid
   AND blocking_locks.objid IS NOT DISTINCT FROM blocked_locks.objid
   AND blocking_locks.objsubid IS NOT DISTINCT FROM blocked_locks.objsubid
   AND blocking_locks.pid != blocked_locks.pid
JOIN pg_catalog.pg_stat_activity blocking ON blocking.pid = blocking_locks.pid
WHERE NOT blocked_locks.granted
ORDER BY blocked_duration DESC;
2. Cài đặt chạy mỗi 1 phút + Thời gian thực
Trong panel đang edit, chuyển sang tab Query options (ở dưới cùng bên trái).
Điền các thông số sau:
Refresh → chọn 1m (hoặc gõ 1m)
Max data points → để mặc định (hoặc 1000)
Cache timeout → để trống
Nhấn Apply.
3. Cài Auto Refresh cho cả Dashboard (bắt buộc)
Ở góc trên bên phải dashboard, nhấn vào đồng hồ (Time range picker).
Chọn Auto refresh → 1m.
Nếu không có 1m sẵn, nhấn Add custom interval → gõ 1m → Save.
→ Từ giờ dashboard sẽ tự động chạy query mỗi 1 phút và cập nhật kết quả live.
4. Tối ưu hiển thị (rất quan trọng để nhìn “thời gian thực”)
Trong tab Field hoặc Overrides:
Cột blocked_duration:
Unit → Duration → Seconds (s)
Color → Red nếu > 30s, Orange nếu > 10s
Cột blocked_query và blocking_query → Cell display mode = Wrapped text
Đổi tiêu đề panel thành: 🔴 LIVE - Blocking Sessions (refresh mỗi 1 phút)
5. Kiểm tra
Nhấn Save dashboard.
Đợi 60 giây hoặc nhấn nút Refresh (mũi tên tròn).
Bạn sẽ thấy bảng tự động cập nhật mỗi 1 phút mà không cần làm gì.
Hoàn tất! Query của bạn giờ đã chạy mỗi 1 phút 1 lần và hiển thị thời gian thực.
Bạn muốn mình:
Gửi luôn JSON của panel để import 1 click?
Thêm Alerting (báo động khi có blocking > 30 giây)?
Hay biến nó thành đồ thị theo thời gian (lịch sử blocking)?
Cứ nói mình biết nhé! 🔥


✅ Đã hiểu!
Bạn muốn tạo query test blocking để kiểm tra panel Grafana (query detect blocking bạn vừa paste).
Dưới đây là bộ query test đơn giản, an toàn, dễ chạy để tạo tình huống blocking thật sự, sau đó panel Grafana sẽ hiển thị ngay (và refresh mỗi 1 phút như bạn đã setup).
📌 Hướng dẫn chạy test (2 session riêng biệt)
Mở 2 cửa sổ terminal/psql/pgAdmin/DBeaver cùng lúc (kết nối cùng database grafana_test).
Session 1 (Giữ lock - Blocking session)
Chạy lần lượt các lệnh sau:
-- 1. Tạo bảng test lock (chạy 1 lần thôi)
CREATE TABLE IF NOT EXISTS lock_test (
    id SERIAL PRIMARY KEY,
    name TEXT
);

-- 2. Chèn dữ liệu mẫu
INSERT INTO lock_test (name) VALUES ('Test Row 1');

-- 3. Bắt đầu transaction và giữ lock (KHÔNG COMMIT)
BEGIN;
UPDATE lock_test 
SET name = 'Locked by Session 1' 
WHERE id = 1;

-- Đừng COMMIT hoặc ROLLBACK ở đây → session này sẽ giữ lock
Để lại session 1 đang chạy (transaction chưa commit).
Session 2 (Bị block - Blocked session)
Chạy lệnh này trong session thứ 2:
-- Session 2: Cố gắng update cùng dòng → sẽ bị block
BEGIN;
UPDATE lock_test 
SET name = 'Trying to lock...' 
WHERE id = 1;

-- Lệnh này sẽ "treo" (blocked) cho đến khi Session 1 commit/rollback
Kết quả mong đợi trong Grafana
Sau khi chạy xong:
Quay lại Grafana → panel Blocking Sessions của bạn.
Đợi 1 phút (auto refresh) hoặc nhấn Refresh.
Bạn sẽ thấy 1 dòng hiện ra:
blocked_pid
blocked_user
blocked_query
blocking_pid
blocking_user
blocked_duration
xxxx
youruser
UPDATE lock_test ...
yyyy
youruser
00:00:15...
→ blocked_duration sẽ tăng dần theo thời gian (đây là “thời gian thực” bạn muốn).
Dọn dẹp sau khi test (rất quan trọng)
Khi test xong, quay lại Session 1 chạy lệnh sau để giải phóng lock:
COMMIT;   -- hoặc ROLLBACK;
Sau đó Session 2 sẽ tự chạy tiếp và bạn có thể COMMIT nó luôn.
Bonus: Query test nhanh chỉ 1 lệnh (nếu bạn lười mở 2 session)
Nếu muốn test siêu nhanh, chạy trong Session 1:
BEGIN;
SELECT pg_sleep(60);  -- giữ lock 60 giây
-- sau đó mở session 2 chạy query update lock_test
Bạn có muốn mình:
Thêm query test nhiều blocking cùng lúc?
Tạo bảng + function tự động sinh blocking mỗi lần chạy?
Hay thêm cột blocked_since vào panel Grafana để hiển thị thời gian bắt đầu block?
Cứ nói mình biết, mình tạo thêm ngay! 🔥



✅ Query đếm số cặp blocking locks (dùng cho Alarm / Alerting)
Dưới đây là query tối ưu chỉ trả về 1 con số — số lượng cặp bị lock (blocking pairs) hiện tại.
Query này được rút gọn từ query chi tiết bạn đang dùng, rất phù hợp để tạo Alert Rule trong Grafana.
1. Query đếm số cặp bị lock (dùng cho Alert)
SELECT 
    COUNT(*) AS blocking_pairs
FROM pg_catalog.pg_locks blocked_locks
JOIN pg_catalog.pg_stat_activity blocked 
    ON blocked.pid = blocked_locks.pid
JOIN pg_catalog.pg_locks blocking_locks 
    ON blocking_locks.locktype = blocked_locks.locktype
   AND blocking_locks.database IS NOT DISTINCT FROM blocked_locks.database
   AND blocking_locks.relation IS NOT DISTINCT FROM blocked_locks.relation
   AND blocking_locks.page IS NOT DISTINCT FROM blocked_locks.page
   AND blocking_locks.tuple IS NOT DISTINCT FROM blocked_locks.tuple
   AND blocking_locks.virtualxid IS NOT DISTINCT FROM blocked_locks.virtualxid
   AND blocking_locks.transactionid IS NOT DISTINCT FROM blocked_locks.transactionid
   AND blocking_locks.classid IS NOT DISTINCT FROM blocked_locks.classid
   AND blocking_locks.objid IS NOT DISTINCT FROM blocked_locks.objid
   AND blocking_locks.objsubid IS NOT DISTINCT FROM blocked_locks.objsubid
   AND blocking_locks.pid != blocked_locks.pid
JOIN pg_catalog.pg_stat_activity blocking 
    ON blocking.pid = blocking_locks.pid
WHERE NOT blocked_locks.granted;
Kết quả trả về:
blocking_pairs = số lượng session đang bị block (mỗi cặp blocked ↔ blocking là 1).
2. Cách dùng trong Grafana để tạo Alarm (cảnh báo)
Mở panel Blocking Sessions của bạn (hoặc Add panel mới).
Chọn Visualization = Stat (hoặc Table cũng được).
Paste query đếm ở trên vào Query editor.
Đặt Panel title: 🔴 Blocking Pairs (Current)
Trong tab Field:
Field blocking_pairs → Unit = None (hoặc Short)
Color mode = Value (để đổi màu theo ngưỡng)
Tạo Alert:
Vào tab Alert (bên trái) → New alert rule
Query: Chọn query blocking_pairs
Condition: WHEN last() OF query IS ABOVE 0
(hoặc > 1, > 2… tùy bạn)
Evaluate every: 1m (đúng với auto refresh bạn đã set)
For: 0s hoặc 30s (thời gian duy trì cảnh báo)
Notification message:
🚨 Có {{ $values.blocking_pairs }} cặp blocking lock trong database!
Nhấn Save → Alert sẽ tự động kiểm tra mỗi 1 phút và gửi cảnh báo nếu có blocking.
3. Bonus: Query đếm + hiển thị thời gian block dài nhất (khuyến nghị)
Nếu bạn muốn alert mạnh hơn (không chỉ đếm số mà còn biết block lâu bao nhiêu):
SELECT 
    COUNT(*) AS blocking_pairs,
    MAX(age(now(), blocked.query_start)) AS longest_block_duration
FROM ... (giữ nguyên phần FROM và WHERE như query trên)
Sau đó trong Alert bạn có thể set condition:
blocking_pairs > 0 AND longest_block_duration > 30s
Bạn muốn mình:
Gửi luôn JSON panel + alert rule để import 1 click?
Hay chỉnh threshold mặc định (ví dụ alert khi > 2 cặp hoặc block > 60s)?
Cứ nói mình biết, mình chỉnh ngay cho bạn! 🔥